# === Demo-only envs (safe to commit this file; DON'T commit a real .env) ===

# If you later connect to GCP, put IDs here and keep .env out of git.
# TODO: replace with your real IDs before cloud deploy (DO NOT COMMIT .env)
GCP_PROJECT_ID=TODO_your_project_id
GCP_REGION=TODO_your_region

# Optional: when you switch storage to GCS or BigQuery
GCS_BUCKET_RAW=TODO_bucket_raw
BQ_DATASET_BRONZE=bronze
BQ_DATASET_SILVER=silver
BQ_DATASET_GOLD=gold
